// Mock data removed — the app now runs on real persisted state (localStorage)
// with live replies from the Hermes gateway. This file is kept only so old
// imports fail loudly; safe to delete.
export {};
